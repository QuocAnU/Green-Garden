/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['vuoncayviet.com', 'example.com'], // Add your allowed domains here
      },
};

export default nextConfig;
