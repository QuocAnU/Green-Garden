/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['vuoncayviet.com', 'example.com', 'images.grove.co'], // Add your allowed domains here
      },
};

export default nextConfig;
